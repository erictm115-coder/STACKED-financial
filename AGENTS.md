# Expo HAS CHANGED

This project targets Expo SDK 54 (downgraded from 56 because the public
Expo Go app on the App/Play Store only supports SDK 54 as of mid-2026 —
55/56 are pending store approval). Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code.
